#include "monty.h"

/**
* instruction_nop - function that does nothing
* @head: double head pointer to the stack
* @line_counter: line count
*
* Return: nothing
*/
void instruction_nop(stack_t **head, unsigned int line_counter)
{
	(void) line_counter;
	(void) head;

}
